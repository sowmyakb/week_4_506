{
  "name": "week4-debug-assignment",
  "version": "1.0.0",
  "description": "Week 4 individual assignment — debug a save/publish race condition",
  "main": "app/server.js",
  "scripts": {
    "start": "node app/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
